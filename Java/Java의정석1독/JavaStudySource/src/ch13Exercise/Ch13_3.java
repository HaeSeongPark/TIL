package ch13Exercise;

public class Ch13_3 {
/*
 * 쓰레드를 일시정지 상태로 만드는 것이 아닌것은?
 * 
 * suspend() : 잠시 멈추는데 자동으로 실행대기 상태 안 됨
 * resume() : suspends를 깨우는 것  X
 * join() : 내 쓰레드에 다른쓰레드 끼워넣는 것 X  // 쓰레드 자신이 하던 작업을 잠시 멈추고 다른 쓰레드가 지정된 시간동안 작업을 수행하도록한다
 * sleep() : 잠시 멈추고 자동으로 실행대기 상태
 * wait() : 동기화관련  락을 가진 쓰레드가 쓰레드를 반납하고 기다리게 한다.
 * notify() : wait에 의해 기다리는 쓰레드를 깨운다. 임의의 쓰레드만 notifyAll()은 모두 X
 */
}
