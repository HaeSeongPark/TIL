package ch14_primitive_reference_array;

public class Note
{
	/*
	 * boolean true / false
	 * char 문자(2byte) 하나, 2byte 양의 정수
	 * int 4byte 정수,
	 * long 8byte 정수 10L 20L
	 * float 4byte 실수 10.5f, 10.5224f
	 * double 8byte 실수, 20.5
	 * 
	 * char[] - 배열도  Object, class를 제공하는 것은 아님
	 * 
	 * 문자열
	 * String : 부모가 Object, 문자열
	 * 
	 * Object - 
	 * 
	 * 기본형 값을 가진다
	 * 참조형은 값을 참조한다
	 * 
	 * int[] array = new int[1000];
	 * int array[] = new int[1000];
	 * 
	 * int[] a,b // a,b가 모두 정수배열
	 * int a[], b // a[] , b는 배열이 아니다
	 * 
	 * Car[] carArray = new Car[3];
	 * carArray[0] = new Car();
	 * carArray[1] = new Car();
	 * carArray[2] = new Car();
	 * 
	 */
}
