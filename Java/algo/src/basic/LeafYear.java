package basic;

public class LeafYear {
	public static void main(String[] args) {
		/*
		 * a+1 ~  b 까지 년도 사이에 윤년 구하기
		 * 
		 * count = b / 4;
		 * count -=(b/100);
		 * count +=(b/400);
		 * count -= a/4;
		 * count +=(a/100);
		 * count -=(a/400);
		 */
	}
}
